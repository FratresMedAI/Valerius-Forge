# Copy this to .env.local (already gitignored) and set values for your local dev.

# Set to true to silently boot into Masters tier — useful for the owner to always
# see the full product without going through the tier modal or localStorage.
# NEVER commit .env.local — it is gitignored.
VITE_CREATOR_MODE=false
